@Library('Jenkins-shared-library') _

def configMap = [
    project : "roboshop",
    component : "catalogue"
]

if( ! env.BRANCH_NAME.equalsIgnoreCase('main')) {
    nodejsVMpipeline(configMap)
}
else {
    echo "Please proceed with PROD Process"
}

